# Optional in production when frontend and backend are on different domains.
# Example: https://smart-attendance-api.up.railway.app
VITE_API_BASE_URL=

# Optional for local dev proxy target override.
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:5000
